#!/bin/bash -xe
sleep 4m
sudo apt update && sudo apt install -y curl unzip
curl -J -O -L https://app.strongdm.com/releases/cli/linux && unzip sdmcli* && rm -f sdmcli*
sudo ./sdm install --relay --token="${SDM_TOKEN}" --user=azureuser